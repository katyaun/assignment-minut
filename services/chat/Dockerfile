FROM node

ENV PORT=
ENV CHAT_DB=
ENV USERS_REMOTE_URL=
ENV PROPERTIES_URL=
ENV MONGO_CONNECTION=
ENV JWT_KEY=

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3004

CMD ["node", "index.js"]

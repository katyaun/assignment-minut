FROM node:alpine

ENV PORT=
ENV USERS_REMOTE_URL=
ENV PROPERTIES_URL=
ENV MONGO_CONNECTION=
ENV JWT_KEY=
ENV RESERVATIONS_DB=

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3003

CMD ["node", "index.js"]
